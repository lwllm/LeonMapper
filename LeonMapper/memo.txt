整数类型：sbyte, byte, short, ushort, int, uint, long, ulong
浮点数类型：float, double, decimal
字符类型：char
布尔类型：bool
指针类型：IntPtr, UIntPtr


public interface IConverter<in TInput, out TOutput>
{
    public TOutput Convert(TInput input);
}
帮我写c#中ulong转成sbyte, byte, short, ushort, int, uint, long, ulong的实现类，不需要别的类型，需要加上范围检查，抛出OverflowException异常


public interface IConverter<in TInput, out TOutput>
{
    public TOutput Convert(TInput input);
}
帮我写c#中ulongt转成float, double, decimal,char,bool,IntPtr, UIntPtr,string的实现类，不需要别的类型，需要加上范围检查，抛出OverflowException异常